server:
  port: 8080  # Različit od Menu Service (koji koristi 8080)

spring:
  datasource:
    url: jdbc:h2:mem:order-db
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect